import 'package:flutter/material.dart';

class menueHomeScreen extends StatelessWidget {
  const menueHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}